namespace ex = "http://expath.org/ns/xmlspec"

include "xmlspec-orig.rnc"

local.spec.att |=
   attribute ex:w3c { xsd:boolean }

eg.pcd.mix |=
   element ex:function { text }
 | element ex:type { text }
